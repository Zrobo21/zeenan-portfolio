name: Auto Blog Post

on:
  schedule:
    - cron: "0 6 * * 0,3"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: auto-blog-post
  cancel-in-progress: false

jobs:
  generate-post:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Generate new blog posts (2-3 per run)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          POSTS_PER_RUN: "3"
          GENERATE_IMAGES: "false"
        run: python scripts/generate_post.py

      - name: Commit and push new post
        run: |
          git config user.name "auto-blog-bot"
          git config user.email "actions@github.com"
          mkdir -p assets/images/blog
          git add _posts/ scripts/keywords.txt _data/review_log.yml assets/images/blog/
          git diff --staged --quiet && echo "No changes to commit" || git commit -m "Auto: new blog post(s)"
          git push
